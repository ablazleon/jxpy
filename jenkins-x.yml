buildPack: python
pipelineConfig:
 pipelines:
  release:
   build:
    steps:
    - sh: pip install --upgrade pip
      name: upgrade
    - sh: pip install -r requirements.txt
      name: install
    - sh: pylint --disable=R,C,W1203 app.py
      name: lint
