buildPack: python
pipelineConfig:
 pipelines:
  release:
   build:
    steps:
    - sh: echo "Setup"
      name: Setup
    - sh: echo "Install"
      name: Install
    - sh: echo "Lint"
      name: Lint      
