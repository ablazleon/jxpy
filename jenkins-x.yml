buildPack: python
pipelineConfig:
 pipelines:
  release:
   build:
    steps:
    - sh: python3 -m venv ~/.devops &&\ source ~/.devops/bin/activate
      name: setup
    - sh: echo "install"
      name: install
    - sh: echo "lint"
      name: lint
